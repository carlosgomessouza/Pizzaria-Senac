CREATE TABLE Client (
ClientID int auto_increment PRIMARY KEY,
Name varchar(280),
Surname varchar(280),
Age int,
CPF varchar(100),
Gender char,
Zipcode varchar(100),
Address varchar(100),
AddressNumber varchar(100)
);